
# Volumes
volumes:
  morsechatgodb:

networks:
  traefik-proxy:
    external: true
  default:

services:
  #nginx-golang webapp
  morse:
    image: ghcr.io/robalb/morsechat:sha-907023816512f0c470550ec0c41960649ead8f61
    expose:
      - '80'
    restart: unless-stopped
    environment:
      PORT: 8080
      SQLITEPATH: app/backend/db/master.sqlite
      SECRET: ${SECRET}
    volumes:
      - morsechatgodb:/app/backend/db
    labels:
      - traefik.enable=true
      - traefik.http.routers.morsego.rule=Host(`morse-dev.halb.it`)
      - traefik.http.routers.morsego.entrypoints=websecure
      - traefik.http.routers.morsego.tls=true
      - traefik.http.routers.morsego.tls.certresolver=myresolver
    networks:
      - traefik-proxy
